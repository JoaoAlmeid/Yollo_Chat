/*import prisma from "../../../prisma/client"

async function seed() {
    await prisma.plan.create({
        data: {
            id: 3124446764396717495,
            name: 'Plan Test',
            users: 20,
            connections: 100,
            queues: 20,
            value: 10
        }
    })
}

seed().then(() => {
    console.log('Database seeded!')
    prisma.$disconnect()
})
*/
